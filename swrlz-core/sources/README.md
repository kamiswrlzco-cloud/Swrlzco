# Source Packages

Each source ZIP must be stored beside its exact `.sha256` sibling.

Current Forge routes for this repository:

- CLIENT: `swrlz-core/sources/client`
- SERVER: `swrlz-core/sources/server`

Do not substitute debug-APK checksums for source-package checksums.
