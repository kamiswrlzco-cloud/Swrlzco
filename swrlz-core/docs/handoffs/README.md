# Handoffs

This directory preserves transition/handoff documents used to establish migration lineage.

The corrected CFv2.1.0 handoff predates the subsequent source-only Forge parity update that
produced CLIENT CFv2.1.2 and SERVER CFv2.1.0. Use `../CURRENT_AUTHORITY.md` for the active
package baseline.
